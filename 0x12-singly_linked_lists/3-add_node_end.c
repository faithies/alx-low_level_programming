#include "lists.h"

list_t *add_node_end(list_t **head, const char *str)
{

unsigned int temp;
unsigned int len = 0;

	new = malloc(sizeof(list_t));
	
	if(new == NULL)
	{
		retrun (NULL);
	}


	while(str[len])
	{
		len++;

	}

	return(*new);
}
