#include <stdio.h>

/**
 * _abs - function name
 * @n: function parameter
 * Return: Always 0 on success
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	return (0);
}
