#include "main.h"
#include <stdio.h>

/**
 * mul - function name
 * @a: function parameter
 * @b: second function parameter
 * result: prints the result of a and b
 * Return: Result on success
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
