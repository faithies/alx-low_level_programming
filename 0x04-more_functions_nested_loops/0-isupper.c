#include "main.h"

/**
 * _isupper - function name
 * @c: function parameter
 * Return: Always on success and 0 otherwise
 */

int _isupper(int c)
{

	if (c == 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
