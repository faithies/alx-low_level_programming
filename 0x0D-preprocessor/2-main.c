#include <stdlib.h>
#include <stdio.h>

/**
 * main - code entry point that prints the name of the
 * file it was complied from
 * Return: Always 0 on its success
 */

int main(void)
{
	printf(__FILE__);
	printf("\n");
	return (0);
}
